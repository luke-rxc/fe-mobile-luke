import styled from 'styled-components';

export const AuthContainerWrapperStyled = styled.div`
  background: ${({ theme }) => theme.color.surface};
  display: flex;
  flex-direction: column;
  padding: 0 2.4rem;
`;
