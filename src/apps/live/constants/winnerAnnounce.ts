// 당첨자 발표 Drawer open & close transition duration
export const transitionDuration = 300;

// 당첨자 발표 open 후 autoScroll 시작 전 delay
export const startDelayDuration = 3000;

// 당첨자 발표 autoScroll 애니메이션 종료 후 close duration
export const closeDuration = 10000;

// 당첨자 발표 content 높이
export const contentHeight = 280;

// 폭죽 로띠 색깔
export const fireworkColor = ['#D65856', '#70CBA6', '#5DAAF8', '#F3D683', '#B97BD7'] as const;

// 폭죽과 폭죽 사이 Duration
export const fireworkDuration = [200, 300, 500, 700, 850] as const;
