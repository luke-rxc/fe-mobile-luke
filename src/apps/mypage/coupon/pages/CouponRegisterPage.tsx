import { CouponRegisterContainer } from '../containers';

/**
 * My Page > 쿠폰 등록 페이지
 */
const CouponRegisterPage = () => {
  return <CouponRegisterContainer />;
};

export default CouponRegisterPage;
