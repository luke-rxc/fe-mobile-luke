import { ClaimCancelFullContainer } from '../containers';

const ClaimCancelFullPage = () => {
  return (
    <>
      <ClaimCancelFullContainer />
    </>
  );
};

export default ClaimCancelFullPage;
