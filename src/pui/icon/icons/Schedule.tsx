import styled from 'styled-components';
import { ReactComponent as schedule } from '@assets/icons/ic_schedule.svg';
import { Icon } from '../Icon';

export const Schedule = styled(Icon).attrs({ icon: schedule })``;
