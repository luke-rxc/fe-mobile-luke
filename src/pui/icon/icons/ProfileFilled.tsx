import styled from 'styled-components';
import { ReactComponent as profileFilled } from '@assets/icons/ic_profile_filled.svg';
import { Icon } from '../Icon';

export const ProfileFilled = styled(Icon).attrs({ icon: profileFilled })``;
