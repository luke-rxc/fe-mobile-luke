import styled from 'styled-components';
import { ReactComponent as back } from '@assets/icons/ic_back.svg';
import { Icon } from '../Icon';

/**
 * @deprecated - `<ArrowLeft />`
 */
export const Back = styled(Icon).attrs({ icon: back })``;
