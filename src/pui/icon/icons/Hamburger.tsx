import styled from 'styled-components';
import { ReactComponent as hamburger } from '@assets/icons/ic_hamburger.svg';
import { Icon } from '../Icon';

export const Hamburger = styled(Icon).attrs({ icon: hamburger })``;
