import styled from 'styled-components';
import { ReactComponent as camera } from '@assets/icons/ic_camera.svg';
import { Icon } from '../Icon';

export const Camera = styled(Icon).attrs({ icon: camera })``;
