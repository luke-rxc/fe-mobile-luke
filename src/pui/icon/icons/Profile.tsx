import styled from 'styled-components';
import { ReactComponent as profile } from '@assets/icons/ic_profile.svg';
import { Icon } from '../Icon';

export const Profile = styled(Icon).attrs({ icon: profile })``;
