import styled from 'styled-components';
import { ReactComponent as cardFilled } from '@assets/icons/ic_card_filled.svg';
import { Icon } from '../Icon';

export const CardFilled = styled(Icon).attrs({ icon: cardFilled })``;
