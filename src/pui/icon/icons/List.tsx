import styled from 'styled-components';
import { ReactComponent as list } from '@assets/icons/ic_list.svg';
import { Icon } from '../Icon';

/**
 * @deprecated
 */
export const List = styled(Icon).attrs({ icon: list })``;
