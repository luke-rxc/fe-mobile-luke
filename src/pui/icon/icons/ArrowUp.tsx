import styled from 'styled-components';
import { ReactComponent as arrowUp } from '@assets/icons/ic_arrow_up.svg';
import { Icon } from '../Icon';

export const ArrowUp = styled(Icon).attrs({ icon: arrowUp })``;
