import styled from 'styled-components';
import { ReactComponent as filter } from '@assets/icons/ic_filter.svg';
import { Icon } from '../Icon';

export const Filter = styled(Icon).attrs({ icon: filter })``;
