import styled from 'styled-components';
import { ReactComponent as lineFilled } from '@assets/icons/ic_line_filled.svg';
import { Icon } from '../Icon';

export const LineFilled = styled(Icon).attrs({ icon: lineFilled })``;
