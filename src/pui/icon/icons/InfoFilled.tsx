import styled from 'styled-components';
import { ReactComponent as infoFilled } from '@assets/icons/ic_info_filled.svg';
import { Icon } from '../Icon';

export const InfoFilled = styled(Icon).attrs({ icon: infoFilled })``;
