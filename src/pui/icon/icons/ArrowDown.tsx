import styled from 'styled-components';
import { ReactComponent as arrowDown } from '@assets/icons/ic_arrow_down.svg';
import { Icon } from '../Icon';

export const ArrowDown = styled(Icon).attrs({ icon: arrowDown })``;
