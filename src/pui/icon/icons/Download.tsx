import styled from 'styled-components';
import { ReactComponent as download } from '@assets/icons/ic_download.svg';
import { Icon } from '../Icon';

export const Download = styled(Icon).attrs({ icon: download })``;
