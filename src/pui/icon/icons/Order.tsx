import styled from 'styled-components';
import { ReactComponent as order } from '@assets/icons/ic_order.svg';
import { Icon } from '../Icon';

export const Order = styled(Icon).attrs({ icon: order })``;
