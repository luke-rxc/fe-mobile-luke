import styled from 'styled-components';
import { ReactComponent as claim } from '@assets/icons/ic_claim.svg';
import { Icon } from '../Icon';

export const Claim = styled(Icon).attrs({ icon: claim })``;
