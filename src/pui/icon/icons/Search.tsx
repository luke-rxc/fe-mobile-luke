import styled from 'styled-components';
import { ReactComponent as search } from '@assets/icons/ic_search.svg';
import { Icon } from '../Icon';

export const Search = styled(Icon).attrs({ icon: search })``;
