import styled from 'styled-components';
import { ReactComponent as logo } from '@assets/icons/ic_logo.svg';
import { Icon } from '../Icon';

/**
 * @deprecated
 */
export const Logo = styled(Icon).attrs({ icon: logo })``;
