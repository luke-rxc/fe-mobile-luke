import styled from 'styled-components';
import { ReactComponent as pin1 } from '@assets/icons/ic_pin-1.svg';
import { Icon } from '../Icon';

export const Pin1 = styled(Icon).attrs({ icon: pin1 })``;
