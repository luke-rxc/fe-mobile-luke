import styled from 'styled-components';
import { ReactComponent as likeFilled } from '@assets/icons/ic_like_filled.svg';
import { Icon } from '../Icon';

export const LikeFilled = styled(Icon).attrs({ icon: likeFilled })``;
