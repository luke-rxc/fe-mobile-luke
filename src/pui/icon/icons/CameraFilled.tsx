import styled from 'styled-components';
import { ReactComponent as cameraFilled } from '@assets/icons/ic_camera_filled.svg';
import { Icon } from '../Icon';

export const CameraFilled = styled(Icon).attrs({ icon: cameraFilled })``;
