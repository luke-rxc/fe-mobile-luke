import styled from 'styled-components';
import { ReactComponent as clipboard } from '@assets/icons/ic_clipboard.svg';
import { Icon } from '../Icon';

/**
 * @deprecated - `<Duplicate />`
 */
export const Clipboard = styled(Icon).attrs({ icon: clipboard })``;
