import styled from 'styled-components';
import { ReactComponent as tossFilled } from '@assets/icons/ic_toss_filled.svg';
import { Icon } from '../Icon';

export const TossFilled = styled(Icon).attrs({ icon: tossFilled })``;
