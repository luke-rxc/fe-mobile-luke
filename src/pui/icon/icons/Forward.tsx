import styled from 'styled-components';
import { ReactComponent as forward } from '@assets/icons/ic_forward.svg';
import { Icon } from '../Icon';

/**
 * @deprecated - `<ArrowRight />`
 */
export const Forward = styled(Icon).attrs({ icon: forward })``;
