import styled from 'styled-components';
import { ReactComponent as price } from '@assets/icons/ic_price.svg';
import { Icon } from '../Icon';

export const Price = styled(Icon).attrs({ icon: price })``;
