import styled from 'styled-components';
import { ReactComponent as calendar } from '@assets/icons/ic_calendar.svg';
import { Icon } from '../Icon';

export const Calendar = styled(Icon).attrs({ icon: calendar })``;
