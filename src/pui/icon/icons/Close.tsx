import styled from 'styled-components';
import { ReactComponent as close } from '@assets/icons/ic_close.svg';
import { Icon } from '../Icon';

export const Close = styled(Icon).attrs({ icon: close })``;
