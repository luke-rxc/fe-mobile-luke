import styled from 'styled-components';
import { ReactComponent as delivery } from '@assets/icons/ic_delivery.svg';
import { Icon } from '../Icon';

export const Delivery = styled(Icon).attrs({ icon: delivery })``;
