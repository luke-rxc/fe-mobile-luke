import styled from 'styled-components';
import { ReactComponent as sendFilled } from '@assets/icons/ic_send_filled.svg';
import { Icon } from '../Icon';

export const SendFilled = styled(Icon).attrs({ icon: sendFilled })``;
