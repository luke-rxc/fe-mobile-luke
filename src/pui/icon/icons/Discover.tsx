import styled from 'styled-components';
import { ReactComponent as discover } from '@assets/icons/ic_discover.svg';
import { Icon } from '../Icon';

export const Discover = styled(Icon).attrs({ icon: discover })``;
