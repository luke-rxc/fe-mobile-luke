import styled from 'styled-components';
import { ReactComponent as stand } from '@assets/icons/ic_stand.svg';
import { Icon } from '../Icon';

export const Stand = styled(Icon).attrs({ icon: stand })``;
