import styled from 'styled-components';
import { ReactComponent as videoFilled } from '@assets/icons/ic_video_filled.svg';
import { Icon } from '../Icon';

export const VideoFilled = styled(Icon).attrs({ icon: videoFilled })``;
