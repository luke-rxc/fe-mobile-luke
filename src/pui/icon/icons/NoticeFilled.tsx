import styled from 'styled-components';
import { ReactComponent as noticeFilled } from '@assets/icons/ic_notice_filled.svg';
import { Icon } from '../Icon';

export const NoticeFilled = styled(Icon).attrs({ icon: noticeFilled })``;
