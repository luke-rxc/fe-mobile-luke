import styled from 'styled-components';
import { ReactComponent as video } from '@assets/icons/ic_video.svg';
import { Icon } from '../Icon';

export const Video = styled(Icon).attrs({ icon: video })``;
