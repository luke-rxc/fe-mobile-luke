import styled from 'styled-components';
import { ReactComponent as setting } from '@assets/icons/ic_setting.svg';
import { Icon } from '../Icon';

export const Setting = styled(Icon).attrs({ icon: setting })``;
