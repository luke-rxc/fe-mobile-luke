import styled from 'styled-components';
import { ReactComponent as empty } from '@assets/icons/ic_empty.svg';
import { Icon } from '../Icon';

export const Empty = styled(Icon).attrs({ icon: empty })``;
