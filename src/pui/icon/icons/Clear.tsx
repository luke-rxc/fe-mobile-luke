import styled from 'styled-components';
import { ReactComponent as clear } from '@assets/icons/ic_clear.svg';
import { Icon } from '../Icon';

export const Clear = styled(Icon).attrs({ icon: clear })``;
