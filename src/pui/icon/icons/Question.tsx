import styled from 'styled-components';
import { ReactComponent as question } from '@assets/icons/ic_question.svg';
import { Icon } from '../Icon';

export const Question = styled(Icon).attrs({ icon: question })``;
