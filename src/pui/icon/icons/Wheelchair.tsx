import styled from 'styled-components';
import { ReactComponent as wheelchair } from '@assets/icons/ic_wheelchair.svg';
import { Icon } from '../Icon';

export const Wheelchair = styled(Icon).attrs({ icon: wheelchair })``;
