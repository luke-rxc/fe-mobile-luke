import styled from 'styled-components';
import { ReactComponent as duplicate } from '@assets/icons/ic_duplicate.svg';
import { Icon } from '../Icon';

export const Duplicate = styled(Icon).attrs({ icon: duplicate })``;
