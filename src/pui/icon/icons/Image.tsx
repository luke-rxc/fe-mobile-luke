import styled from 'styled-components';
import { ReactComponent as image } from '@assets/icons/ic_image.svg';
import { Icon } from '../Icon';

export const Image = styled(Icon).attrs({ icon: image })``;
