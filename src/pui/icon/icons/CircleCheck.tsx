import styled from 'styled-components';
import { ReactComponent as circleCheck } from '@assets/icons/ic_circle_check.svg';
import { Icon } from '../Icon';

export const CircleCheck = styled(Icon).attrs({ icon: circleCheck })``;
