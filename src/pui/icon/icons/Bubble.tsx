import styled from 'styled-components';
import { ReactComponent as bubble } from '@assets/icons/ic_bubble.svg';
import { Icon } from '../Icon';

export const Bubble = styled(Icon).attrs({ icon: bubble })``;
