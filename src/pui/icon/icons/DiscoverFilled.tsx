import styled from 'styled-components';
import { ReactComponent as discoverFilled } from '@assets/icons/ic_discover_filled.svg';
import { Icon } from '../Icon';

export const DiscoverFilled = styled(Icon).attrs({ icon: discoverFilled })``;
