import styled from 'styled-components';
import { ReactComponent as pauseFilled } from '@assets/icons/ic_pause_filled.svg';
import { Icon } from '../Icon';

export const PauseFilled = styled(Icon).attrs({ icon: pauseFilled })``;
