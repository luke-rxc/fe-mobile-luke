import styled from 'styled-components';
import { ReactComponent as option } from '@assets/icons/ic_option.svg';
import { Icon } from '../Icon';

export const Option = styled(Icon).attrs({ icon: option })``;
