import styled from 'styled-components';
import { ReactComponent as priceFilled } from '@assets/icons/ic_price_filled.svg';
import { Icon } from '../Icon';

export const PriceFilled = styled(Icon).attrs({ icon: priceFilled })``;
