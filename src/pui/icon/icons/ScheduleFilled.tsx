import styled from 'styled-components';
import { ReactComponent as scheduleFilled } from '@assets/icons/ic_schedule_filled.svg';
import { Icon } from '../Icon';

export const ScheduleFilled = styled(Icon).attrs({ icon: scheduleFilled })``;
