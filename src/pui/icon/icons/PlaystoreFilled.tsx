import styled from 'styled-components';
import { ReactComponent as playStoreFilled } from '@assets/icons/ic_playstore_filled.svg';
import { Icon } from '../Icon';

export const PlayStoreFilled = styled(Icon).attrs({ icon: playStoreFilled })``;
